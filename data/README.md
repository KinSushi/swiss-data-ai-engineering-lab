# Data formats

This folder contains small synthetic examples of the formats a Data & AI engineer should understand.

- `raw/`: small text-based samples that are safe to track.
- `schemas/`: JSON Schema, Avro schema, Protobuf, OpenAPI, AsyncAPI and GraphQL schema examples.
- `processed_examples/`: notes for binary or table formats that should usually be generated locally.
