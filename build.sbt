ThisBuild / scalaVersion := "2.13.14"
ThisBuild / organization := "ch.kinsushi.lab"
ThisBuild / version := "0.1.0"

lazy val root = (project in file(".")).settings(
  name := "swiss-data-ai-engineering-lab"
)
