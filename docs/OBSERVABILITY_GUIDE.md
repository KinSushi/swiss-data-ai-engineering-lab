# Observability guide

A model in production is not just a model. It is a service that must be monitored.

Core signals:

- data freshness
- missing-value rate
- duplicate transaction rate
- invalid-currency count
- API latency
- API error rate
- prediction volume
- drift status
- audit trail completeness

Examples are stored under `observability/`.
